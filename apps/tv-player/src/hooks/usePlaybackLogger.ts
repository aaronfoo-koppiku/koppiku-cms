import type { PlaylistItem, Media } from '@koppiku/shared'

export function usePlaybackLogger(_currentItem: (PlaylistItem & { media: Media }) | null, _deviceId: string): void {
  // stub — implemented in Task 16
}
